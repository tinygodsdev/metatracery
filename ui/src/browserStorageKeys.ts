/** Re-export so all app localStorage keys live in one place. */
export { COLOR_SCHEME_STORAGE_KEY } from './colorSchemeStorage';

/** Full grammar library blob (versioned JSON). */
export const GRAMMAR_LIBRARY_STORAGE_KEY = 'lemula-gge-saved-grammars';

/** sessionStorage: user acknowledged "browser-only storage" disclaimer this tab session. */
export const STORAGE_DISCLAIMER_SESSION_KEY = 'lemula-gge-storage-disclaimer-ack';
