# Optional: override the public site URL for canonical links, meta tags, sitemap, and Open Graph (no trailing slash).
# Defaults to https://grammar.tinygods.dev if unset.
# VITE_PUBLIC_SITE_URL=https://grammar.tinygods.dev
